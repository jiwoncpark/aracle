group: travis_latest

language: python

python:
    - 3.6

compiler:
    - g++
   
cache:
    ccache: true
    pip: true

before_install:
    - pip install --upgrade pip
    - pip install cython

install:
    - pip install -e .

script:
    - python setup.py test

after_success:
    - codecov